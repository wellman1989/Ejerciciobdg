/* const routes = require('./routers');
const pgk = require('../package.json');

exports.register = (server, Option, next) => {
  server.route(routes(Option));
  next();
};

exports.register.attributes = {
  pgk,
};
*/
